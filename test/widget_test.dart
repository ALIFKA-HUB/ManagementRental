import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Phase 1 placeholder test', (WidgetTester tester) async {
    // Widget tests akan ditambah per phase
    expect(true, isTrue);
  });
}
